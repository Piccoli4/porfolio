<div class="text-center mb-12">
  <h2 class={`
    flex 
    justify-center 
    items-center 
    gap-4
    text-3xl 
    md:text-4xl
    font-bold
    bg-gradient-to-r 
    from-indigo-600 
    via-purple-600 
    to-indigo-600
    bg-clip-text 
    text-transparent
    bg-[length:200%_100%]
    animate-gradient-x
    mb-4
    ${Astro.props.className || ''}
  `}>
    <slot />
  </h2>
  
  <!-- Línea decorativa -->
  <div class="flex justify-center">
    <div class="w-24 h-1 bg-gradient-to-r from-transparent via-indigo-500 to-transparent rounded-full opacity-60"></div>
  </div>
</div>

<style>
  @keyframes gradient-x {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }
  
  .animate-gradient-x {
    animation: gradient-x 3s ease-in-out infinite;
  }
</style>